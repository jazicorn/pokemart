import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => (
    <div>
        <Head>
            <title>Project</title>
            <meta name="description" content="...about" />
        </Head>

        <main>
            <h1>Hello World</h1>
        </main>
    </div>
);

export default Home;
